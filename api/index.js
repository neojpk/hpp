import stories from "./stories.json";

export const fetchUserRoom = async () => {
  // this is where we should fetch the user room details
  // like stories, points, anything related to the votation
  return {
    points: [1, 2, 3, 5, 8, 13, 21],
    stories,
  };
};
